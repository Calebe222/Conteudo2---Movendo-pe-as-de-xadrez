#include <stdio.h>


int main () {

    // Loop que começa em 0 e vai até 100, incrementando de 10 em 10
    for (int i = 0; i <= 100; i += 10) {
        // Imprime o valor atual de i
        printf("%d\n", i);
    }

    return 0;
}